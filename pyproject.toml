# Python side of the two-toolchain root (ADR-0003): the FR-4 inspection engine.
# Not a distributable package (tool.uv.package=false) — imports resolve from the
# repo root (pythonpath), mirroring the src/modules/<context> layout (ARC-001).
[project]
name = "secure-ga4-bq-template"
version = "0.1.0"
description = "Secure GA4->BigQuery mart asset: governed build rails + deterministic inspection engine (FR-4)"
requires-python = ">=3.12"
dependencies = [
    # ADR-0003 Option 2: one discovery client covers all four read-only APIs
    # (BigQuery metadata, Resource Manager IAM, Data Catalog, Logging config).
    "google-api-python-client>=2.100,<3",
    "google-auth>=2.23,<3",
    # Catalog + engagement-parameter files are YAML (catalog/ga4-sensitivity.yml).
    "pyyaml>=6.0,<7",
    # ADR-0004: official Vertex AI client, isolated in reporting infrastructure.
    # Major upgrades require an explicit external-contract review.
    "google-genai>=2.10,<3",
]

[dependency-groups]
dev = [
    "pytest>=8.0",
    "pytest-cov>=5.0",
    "ruff>=0.8",
    "mypy>=1.13",
    "types-pyyaml>=6.0",
]

[tool.uv]
package = false
# Keep the transitive crypto backend at the supported compatibility floor without making
# it a direct application dependency.
constraint-dependencies = ["cryptography>=50.0.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]

[tool.ruff]
line-length = 100
target-version = "py312"
# Terraform/profile dirs carry no Python; keep ruff scoped to executable Python and tests.
include = ["src/**/*.py", "scripts/**/*.py", "tests/**/*.py"]
extend-exclude = ["scripts/template_sync_auth.py", "scripts/template_inheritance.py"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]

[tool.mypy]
python_version = "3.12"
strict = true
mypy_path = "."
namespace_packages = true
explicit_package_bases = true

# googleapiclient/google.auth ship no type information; the untyped surface is
# confined to infrastructure/gcp (COD-041) — adapters receive services as Any
# and return typed domain records.
[[tool.mypy.overrides]]
module = ["googleapiclient.*", "google.auth", "google.auth.*"]
ignore_missing_imports = true
